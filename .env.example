# Frontend (Vite) — set after running `npx convex dev` once
VITE_CONVEX_URL=

# Set these as CONVEX env vars, not here — run:
#   npx convex env set ANTHROPIC_API_KEY sk-ant-...
#   npx convex env set NOTION_TOKEN secret_...
#   npx convex env set NOTION_HIRING_DB_ID ...
#   npx convex env set NOTION_POLICY_DB_ID ...
#
# ANTHROPIC_API_KEY=
# NOTION_TOKEN=
# NOTION_HIRING_DB_ID=
# NOTION_POLICY_DB_ID=
